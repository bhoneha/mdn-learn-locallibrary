extends layout

block content 
  h1 #{title}
  p #[strong Are you sure to delete this bookinstance?]

  form(method='POST')
    div.form-group
      input#authorid.form-control(type='hidden', name='bookinstanceid', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete Book Instance